/* ============================================================
 * CircleChat 私人聊天 — 前端配置（显示地址与请求地址分离）
 * ============================================================ */

window.CHAT_CONFIG = {
  /* 请求地址：所有 API 与 WebSocket 连接使用的前缀。
   * 同源部署留空字符串 ''；若经反代挂子路径可写 '/chat'；
   * 若前后端跨域可写完整地址如 'https://api.example.com'。 */
  apiBase: '',

  /* 展示地址：用于页面展示/复制分享的服务器地址。
   * 同源留空，将自动取当前页面地址。 */
  displayBase: ''
};
